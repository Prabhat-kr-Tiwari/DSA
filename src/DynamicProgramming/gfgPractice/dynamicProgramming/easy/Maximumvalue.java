package DynamicProgramming.gfgPractice.dynamicProgramming.easy;

public class Maximumvalue {

 /*   Given an array arr[] of integres.

    Find the maximum value of arr[j] – arr[i] + arr[l] – arr[k], such that
        i < j < k < l.*/
    public int findMaxValue(int[] arr) {
        // code here
        if(arr.length<4){
            return -1;
        }
        for (int i = 0; i < arr.length ; i++) {
//            if()
        }
        return 1;
    }
    public static void main(String[] args) {

    }
}
